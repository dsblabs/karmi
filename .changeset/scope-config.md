---
"@karmi/core": patch
---

Scope keys and the ScopeConfig Durable Object: `karmi.scope(id)` with `config.get/set` revisions and compare-and-set, `agents.put/get/list/history/delete/validate` with versioned Agent Specs validated against the Deployment defaults and the Scope, and `status/suspend/resume/destroy/destroyStatus` lifecycle states.

---
"@karmi/core": patch
---

Monorepo and `@karmi/core` skeleton: `createKarmi`, `define*` constructors, Catalogue assembly with `describe()`, fixed `KARMI_*` bindings, compatibility-date floor and the published wrangler baseline.

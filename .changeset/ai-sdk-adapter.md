---
"@karmi/ai-sdk": minor
"@karmi/core": patch
---

Add the AI SDK V4 doStream Provider adapter, preserving replay metadata, provider-executed parts, raw events and reported gateway/OpenRouter costs. Retain metadata only within the model or provider scope of its content block.

---
"@karmi/core": patch
---

Parking: Permission Policy `ask` parks the Turn per asked call with `thread.approve(seq, answer)`, timeouts and remembered allows; a `longRunning` budget of Steps, wall time and tokens that parks for a `continue` Approval; the Job seam (`{ pending: jobId }` and `thread.jobs`); `thread.cancel()`, `send(input, { steer: true })`, coalesced mid-Turn inputs and `thread.resume()` after a Scope suspension.
